# Best Practices Examples

Add small example folders demonstrating a practice, e.g.:

- `examples/python-project/` — repo layout, linting, and CI
- `examples/workflow/` — minimal workflow templates

When adding an example, include a short `README.md` explaining why the pattern is recommended.
